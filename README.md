# Cifar10_Image_Classification__With_K-means_Unique--desin29
This project demonstrates image classification on the CIFAR-10 dataset using a custom-built K-Nearest Neighbors (KNN) algorithm implemented entirely from scratch in Python and NumPy.
